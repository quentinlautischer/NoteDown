{
  "name": "NoteDown",
  "description": "Note Taking App",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "undefined"
  },
  "main": "none.js",
  "scripts": {
    "massinstall": "npm install & cd server && npm install && cd .. & cd server/db && npm install && cd ../.. & cd desktop && npm install && cd .. & cd mobile && npm install && cd ..",
    "desktop": "cd desktop && electron .",
    "server": "cd server && node server.js",
    "db": "",
    "ios_launch": "cd mobile && react-native run-ios",
    "android_launch": "cd mobile && react-native run-android"
  },
  "babel": {
    "presets": [
      "es2015",
      "react"
    ]
  },
  "dependencies": {
    "mongoose": "^4.8.2"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "fs": "0.0.1-security",
    "shelljs": "^0.0.9"
  },
  "license": "MIT"
}
